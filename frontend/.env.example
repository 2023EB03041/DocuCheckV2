# Frontend environment variables — copy to `.env` and adjust.
# `.env` is gitignored; never commit real values.

# Base URL of the backend API.
# Local development:
VITE_API_URL=http://localhost:5000/api
# Production example:
# VITE_API_URL=https://your-backend-host/api
